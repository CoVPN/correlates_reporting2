# Mediators of Vaccine Efficacy {#cop-mediators}

```{r cop-mediator-setup, echo = FALSE, message = FALSE, warning = FALSE, results="asis"}
library(here)
config <- config::get(config = Sys.getenv("TRIAL"))
cat(config)
if(!inherits(NULL, config$study_name)){
	if(config$study_name == "ENSEMBLE"){
		all_COR <- c("D29IncludeNotMolecConfirmed", "D29IncludeNotMolecConfirmedstart1")
	}else if(config$study_name == "COVE"){
		all_COR <- c("D29")
	}else if(config$study_name == "AZD1222"){
		all_COR <- c("D29", "D57")
	}
}else{
	if(config$study_name == "HVTN705"){
		all_COR <- c("D210")
	}
}

for(COR in all_COR){
	res = knitr::knit_child(
  	here::here('cop_mediation', 'report_sub.Rmd')
	)
  cat(res, sep = '\n')
}
```

