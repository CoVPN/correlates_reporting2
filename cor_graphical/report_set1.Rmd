\begin{figure}[H]
\center

\includegraphics[width=1\textwidth]{`r here("cor_graphical", "output", attr(config,"config"), COR, paste0(a, "_by_case_non_case_at_", tp, ".pdf"))`}

\caption{`r if (study_name!="NextGen_Mock") paste0("Distribution of ", aLabel, " at ", tpLabel, COR_postfix_label)                            else if (study_name=="NextGen_Mock") paste0("\\\\Figure 2", ifelse(tp == "Day 1", 0, ifelse(tp == "Day 31", 1, ifelse(tp == "D31 fold-rise over D1", 2, 999))), ". Violin boxplots of ", aLabel, " at ", tpLabel, " ", specimen_type, ", for the (A) Investigational Vaccine arm (blue points) and the (B) Comparator Vaccine arm (green points).  Cases acquired a COVID-19 endpoint 7 days post D31 through {t0} days post D31 (vaccination-proximal cases) or {t0}+1 through {t1} days post D31 (vaccination-distal cases), as designated in the key at the bottom of each panel. Non-cases have no evidence of SARS-CoV-2 infection (i.e., never tested nucleic acid amplification/PCR positive) after D01 up to the date by which the last enrolled participant reached {t1} days post D31 ({enter date}).  Violin boxplots contain interior box plots with upper and lower horizontal edges the 25th and 75th percentiles of antibody level and middle line the 50th percentile, and vertical bars the distance from the 25th (or 75th) percentile of antibody level and the minimum (or maximum) antibody level within the 25th (or 75th) percentile of antibody level minus (or plus) 1.5 times the interquartile range. Each side shows a rotated probability density (estimated by a kernel density estimator with a default Gaussian kernel) of the data ", cohort_name, "AU, Arbitrary Units. ", if (grepl("bind", a)) "bAb, binding antibodies. ", "LLOQ, lower limit of quantitation. ULOQ, upper limit of quantitation. ", if (grepl("bindN", a)) "N, Nucleocapsid protein.")`}

\end{figure}

\hphantom{Random text to appease the latex god}
