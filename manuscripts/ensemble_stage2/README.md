# Assume that we have R 4.4.0 installed
# a) In line 3 EnsembleStage2-Master.R restore R package dependencies

# b) edit all the R script EnsembleStage2-Master.R so that references to the variable spath points to the folder including all R scripts, data.path points to a local copy of the data.

# c) edit all the R script EnsembleStage2-Master.R so that references to the variable spath points to the folder including all R scripts, data.path points to a local copy of the data.

# d) generate report pdfs
Rscript EnsembleStage2-Master.R
