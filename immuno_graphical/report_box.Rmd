\begin{figure}[H]
\center

\includegraphics[width=1\textwidth]{`r here("immuno_graphical", "output", attr(config,"config"), paste0("boxplots_", tp, "_x_trt_", ifelse(study_name == "VaxArt_Mock", paste0(pn, "_"), ""), bsero, "_", study_name, if (study_name == "VaxArt_Mock" & tp %in% c("B", "Day31", "Day181")) "_final", if (study_name == "VaxArt_Mock" & tp %in% c("Day91", "Day366")) "_initial", ".pdf"))`}

\caption{`r if (study_name!="VaxArt_Mock") paste0("Boxplots of ", tpLabel, ifelse(pn!="", paste0(" ", gsub("_", " ", pn)), " Ab"), " markers: ", bseroLabel, " x randomization arm", gender_lb, ". ", ifelse(grepl("Delta", tp), "", neut.marker.captions), immuno_postfix_label)                       else if (study_name == "VaxArt_Mock") paste0("\\\\Figure 1", ifelse(tp == "B", 0, 1), ". Violin boxplots of ", ifelse(grepl("IgG", pn), paste0("bAb-IgG Spike and bAb-IgG Nucleocapsid concentrations "), ifelse(grepl("IgA", pn), paste0("bAb-IgA Spike concentrations "), ifelse(grepl("pseudoneutid50", pn), "PsV nAb ID50 markers ", ifelse(pn == "T4_T8", "CD4+ and CD8+ T-cell markers ", "")))), "at ", gsub("ay", "", gsub("^B","D01", tp)), " ", specimen_type, ", for the Investigational Vaccine arm (blue points) and the Comparator Vaccine arm (green points). ",  ifelse(grepl("IgG", pn), "Panels (A)-(I) show data for Spike antigens Index, Delta AY.4, BA.5, BA.2.86, XBB.1.5, JN.1, KP.2, KP.3, LB.1; Panel (J) for Spike cross-reactivity score; ", ifelse(grepl("pseudoneutid50", pn),  "Panels (A)-(C) show data for Antigens Reference, KP.2, XBB.1.5.", "")), if (grepl("IgG", pn)) "Panel (K) for Nucleocapsid antigen Index. ", "Violin boxplots contain interior box plots with upper and lower horizontal edges the 25th and 75th percentiles of antibody level and middle line the 50th percentile, and vertical bars the distance from the 25th (or 75th) percentile of antibody level and the minimum (or maximum) antibody level within the 25th (or 75th) percentile of antibody level minus (or plus) 1.5 times the interquartile range. Each side shows a rotated probability density (estimated by a kernel density estimator with a default Gaussian kernel) of the data ", cohort_name, ifelse(!grepl("T4|T8", pn), "Upper and lower horizontal lines are the upper and lower limits of quantitation (ULOQ, LLOQ), respectively. Values below the LLOQ are assigned value LLOQ/2. ", ""), ifelse(grepl("IgA|IgG", pn), "AU, Arbitrary Units. bAb, binding antibodies. N, Nucleocapsid protein. ", ifelse(grepl("pseudoneutid50", pn), "nAb, neutralizing antibody. ", "")))`}

\end{figure}

\hphantom{Random text to appease the latex god}
